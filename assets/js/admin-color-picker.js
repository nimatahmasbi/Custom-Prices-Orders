(function( $ ) {
    'use strict';
    // این کد، انتخاب‌گر رنگ وردپرس را روی فیلدهای ورودی با کلاس مشخص شده فعال می‌کند.
    $(function() {
        $('.cpo-color-picker').wpColorPicker();
    });
})( jQuery );
